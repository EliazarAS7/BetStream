<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="BetStream"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BetStream"
        tools:targetApi="35">

        <!-- Actividad principal que lanza la app (MAIN/LAUNCHER) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad de inicio de sesión -->
        <activity
            android:name=".IniciarSesionActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat">
        </activity>

        <!-- Actividad para crear cuenta -->
        <activity
            android:name=".CreateAccountActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat">
        </activity>

        <!-- Actividad de slots (sin intent-filter) -->
        <activity
            android:name=".SlotsActivity"
            android:exported="true"
            android:label="BetStream"
            android:theme="@style/Theme.AppCompat">
        </activity>

        <!-- Actividad para la página principal (Home) -->
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat">
        </activity>

    </application>

</manifest>
